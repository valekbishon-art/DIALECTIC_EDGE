# Новые спот-фичи + визуал (build spot-only)

Только лонг-спот, без плеча/шорта/деривативов. В выводе для пользователя — никакой
посторонней терминологии, чистый рынок. Каждая фича стоит на бэктесте (см. отчёт).

## Команды
| Команда | Что делает | Бэктест-обоснование |
|---|---|---|
| `/stocks [sma]` | Скринер акций из курируемого вотчлиста: кто в аптренде (price>SMA) + сила по полугодовому моментуму. Топ-пики со спарклайнами. | Акции, XS-моментум lb90: **CAGR +42%, Sharpe 1.30**; моментум+тренд — +28% при вдвое меньшей просадке. |
| `/trend [sma]` | Крипто-тренд: какие крупные спот-монеты сейчас выше SMA (держать равным весом), какие в стейбл. Спарклайны цены. | Крипта, тренд SMA50: режет просадку с **−84%** (buy&hold) до **−55%** при сравнимой доходности. |
| `/dca <деп> [транши] [дни]` | План усреднения входа: равные транши с интервалом, прогресс-бары. | DCA сглаживает цену входа — снижает дисперсию исхода против разового входа. |

## Визуальный набор (`ui_kit.py`)
Чистый stdlib, без сети, покрыт тестами:
- `sparkline()` — юникод-мини-график ряда (`▁▂▃▄▅▆▇█`);
- `bar()` — прогресс/сила блоками (`█░`);
- `trend_arrow()` / `chip()` / `pct()` — стрелки и проценты с типографским минусом;
- `rank_emoji()` — медальки 🥇🥈🥉 для топ-листов;
- `card()` — аккуратная карточка (заголовок/строки/подвал).

## Модули
- `ui_kit.py` — визуальные помощники.
- `halal_signals.py` — bot-facing билдеры карточек (`build_stocks_card`, `build_crypto_trend_card`, `build_dca_plan`), фетч дневных close c Yahoo (без ключа) через `asyncio.to_thread`.
- Тесты: `tests/test_halal_signals_ui.py` (13 шт., включая проверку «нет посторонней терминологии в выводе»).

## Меню
Добавлены кнопки нижнего меню «📈 Акции» и «🧭 Тренд», плюс пункты в `/`-меню (`set_my_commands`).

## Не сделано / дальше
- Картинки-графики (matplotlib PNG) прямо в сообщениях бота (сейчас спарклайны текстом — быстро и без зависимостей).
- Forward-test логирование P2P/спот-арба (execution-edge, на дневных свечах не бэктестится).
