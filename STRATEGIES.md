# 📊 Торговая система — стратегии и бэктесты (спот, только лонг)

> Сводка по чистой спот-лонг системе (без плеча/шорта/деривативов/процента). Все инструменты
> с нейтральным выводом — в продукте/боте ноль лишней терминологии. Обновлено: 2026-06-17.

## Ядро: трендовая система (`trend_signals.py`)

Держим спот-актив пока цена > SMA(N), иначе — в стейбле (0%). Юниверс из `asset_filter.py`.

**Бэктест (28 активов, 5.4 года дневных, кост смены 0.20%):**

| окно | CAGR | maxDD | vs buy&hold |
|------|-----:|------:|-------------|
| **SMA50** | **+18.9%** | **−54.8%** | buy&hold +19.9% / **−83.7%** |
| SMA100 | +2.8% | −59.0% | — |
| SMA200 | +6.5% | −58.5% | — |

**Вывод:** снижение просадки **~25-29 п.п. робастно** по всем окнам; **SMA50** сохраняет
доходность почти как buy&hold, но с **вдвое меньшей просадкой** (−55% против −84%). В медвежий
2022 трендовая выходит в кэш. Это не «альфа», а **риск-менеджмент длинной позиции** — реальный,
простой, разворачивается как есть. Лучшее окно — SMA50.

## Фильтр активов (`asset_filter.py`)
Отбор по экономике токена: ELIGIBLE (утилити/платёж/инфра, 28 шт.) vs EXCLUDED (15 шт.:
кредит-протоколы, спекулятивные мемы, гемблинг, миксеры, биржевой BNB). PoS-сети помечены
spot-only (доход-стейкинг не используем). `eligible_universe()` питает трендовую систему.

## Скринер акций (`stock_screener.py`)
Секторный скрин (исключаем алко/табак/казино/банки/страхование/оружие/адалт) + балансовые пороги
(долг/кап <33%, кэш/кап <33%, примесь выручки <5%) на курируемом вотчлисте (~28), затем тренд
SMA50 по дневным с Yahoo. Сейчас 17/28 в аптренде (AMAT, AMD…). Коэффициенты сверять по отчётам.

## Спот-арбитраж (`scripts/spot_arb_scanner.py`)
Cross-exchange спот-спред Gate↔MEXC. Честно: на ликвиде спред ~0 (биржи арбят друг друга);
реальные спреды — на тонких/неликвидных парах + коллизии тикеров (фильтруются). Эдж в исполнении.

## Что НЕ вошло (расстреляно в исследовании, см. историю)
Прогноз цены/пампа, OI, CVD, XS-реверсал, стат-арб — не торгуются после костов. Funding/basis
carry и кросс-арб фандинга давали премию, но это деривативы/процент → убраны из системы.

---

## Воспроизвести
```bash
py asset_filter.py            # фильтр допустимости активов
py trend_signals.py           # трендовый сигнал сейчас + бэктест
py trend_signals.py --sma 100 # другое окно
py stock_screener.py          # скрин акций + тренд (Yahoo)
py scripts/spot_arb_scanner.py# cross-exchange спот-спред
```
